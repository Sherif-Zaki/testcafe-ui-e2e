import dotenv from 'dotenv';

dotenv.config();

export const loginUser = {
  validUserName: 'performance_glitch_user',
  validpassword: 'secret_sauce',
  invalidPassword: 'jwehvf',
};

export const userInfo = {
  firstName: 'test',
  lastName: 'user',
  zipCode: '11111',
};
